# {{功能名称}}功能规格

## 目标

一句话描述用户完成什么业务结果。

## 当前问题

{{当前用户如何工作、失败代价和证据是什么？}}

## 产品边界与证据

- 目标客户/角色：{{谁有这个问题}}
- 当前事实来源：{{数据库/API/事件/明确 Demo}}
- Build / Buy / Integrate：{{为什么由 Nora 实现或集成}}

## 用户与权限

| 角色 | 可执行动作 | 数据范围 |
| --- | --- | --- |
| {{角色}} | {{动作}} | {{范围}} |

## 核心用例

1. {{前置条件}}
2. {{用户动作}}
3. {{系统结果}}

## 页面入口与出口

| 用户任务 | 入口 | 成功出口 | 取消/返回 | 独立查看入口 |
| --- | --- | --- | --- | --- |
| {{任务}} | `{{route}}` | `{{route}}` | `{{route}}` | `{{route}}` |

## 状态与不变量

- 状态：`{{state_a}} → {{state_b}}`
- 不变量：{{任何时候都必须成立的规则}}
- 并发策略：{{乐观锁/唯一约束/幂等键}}

复杂跨对象流程必须另外复制 [工作流规格模板](workflow-spec.md)，不能只用页面状态描述业务状态。

## 数据模型

| 实体 | 关键字段 | 所有权 | 生命周期 |
| --- | --- | --- | --- |
| {{实体}} | {{字段}} | {{模块}} | {{生命周期}} |

## API

列出查询、命令、错误码和示例，详细内容放入 API 契约文档。

## 非功能要求

- 性能：{{分页规模/响应时间}}
- 审计：{{需要记录的操作}}
- 安全：{{身份、组织边界、敏感字段}}
- 可观测性：{{日志、指标}}
- 运行模式：{{演示、开发、生产的差异}}
- 无障碍/响应式：{{键盘、触控、移动降级}}

## 失败、迁移与恢复

- 离线/依赖不可用：{{用户看到什么，是否允许重试}}
- 历史数据：{{迁移、回填或失败关闭}}
- 回滚：{{代码回滚和数据恢复方式}}
- 发布：{{开关、灰度、监控和停止条件}}

## 不在本次范围

- {{明确排除项}}

## 验收标准

- [ ] Given {{前置事实}} / When {{用户动作}} / Then {{可观察业务结果}}。
- [ ] 重复提交、失败回滚、权限/组织边界和离线行为已验证。
- [ ] Demo 与生产能力在界面和数据路径上可区分。

## 必需证据

- 自动化测试：{{Policy/Service/API/UI}}
- 数据库：{{约束、migration、seed、查询证据}}
- 浏览器：{{桌面/移动/错误/离线截图}}
- 文档：{{契约、状态机、ADR、运行手册}}
