# {{模块}} API 契约

## `{{METHOD}} /api/v1/{{path}}`

用途：{{用例}}

权限：{{角色与数据范围}}

### 请求

```json
{}
```

### 成功响应

状态码：`200/201`

```json
{}
```

### 失败响应

| 状态码 | 业务场景 | 稳定错误标识 |
| --- | --- | --- |
| 400 | 输入不合法 | `{{CODE}}` |
| 404 | 资源不存在 | `{{CODE}}` |
| 409 | 状态或并发冲突 | `{{CODE}}` |

### 兼容性

- 新增可选字段：兼容。
- 删除、重命名、改变语义：破坏性变更，必须升级版本或提供迁移期。
