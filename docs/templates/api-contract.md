# {{模块}} API 契约

## `{{METHOD}} /api/v1/{{path}}`

用途：{{用例}}

权限：{{角色与数据范围}}

运行模式：{{demo / development / production}}

幂等键：`{{header_or_field}}`

并发控制：{{revision / ETag / database constraint}}

### 请求

```json
{}
```

### 成功响应

状态码：`200/201`

```json
{}
```

### 失败响应

| 状态码 | 业务场景 | 稳定错误标识 |
| --- | --- | --- |
| 400 | 输入不合法 | `{{CODE}}` |
| 404 | 资源不存在 | `{{CODE}}` |
| 409 | 状态或并发冲突 | `{{CODE}}` |
| 422 | 业务不变量不满足 | `{{CODE}}` |

### 副作用与事务

- 原子写入：{{业务对象、审计事件、派生对象}}
- 失败回滚：{{不得留下的部分状态}}
- 重试结果：{{相同幂等键的稳定响应}}
- 审计身份：{{从认证上下文取得，不接受的客户端伪造字段}}

### 兼容性

- 新增可选字段：兼容。
- 删除、重命名、改变语义：破坏性变更，必须升级版本或提供迁移期。
- Decimal、日期、枚举和错误码必须给出稳定序列化格式。
