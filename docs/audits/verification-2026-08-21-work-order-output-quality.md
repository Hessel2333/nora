# 2026-08-21 工单产出、质量与成品入库验证记录

## 结论

首个单批次产出质量闭环通过验证：生产中工单申报产出后只形成待检产出与待检批次；合格判定原子追加质量记录、成品入库流水、余额，并完成工单与生产批次；不合格判定进入异常且不产生库存。

本结论不代表实际工序投料、损耗/报废、部分报产、返工路线、质量标准主数据、采购收货、production 身份/RBAC 或完整双向追溯已经完成。

## 自动化验证

- `pnpm typecheck`：通过。
- `pnpm lint`：通过。
- `pnpm test`：Web 105 项、API 94 项，共 199 项通过。
- `pnpm build`：36 个 Next.js 路由构建通过。
- `pnpm build:api`：NestJS 构建通过。
- `pnpm test:smoke`：订单/BOM、工单状态、领退料、库存与幂等 smoke 通过。
- `pnpm docs:check`：文档链接通过。

## 数据库与真实 API

- 从空 PostgreSQL 执行 16 个 migration，随后 seed 连续执行两次通过；临时验证数据库已删除。
- 本地既有数据库执行增量 migration 与重复 seed 通过。
- 报产 `9.500 份` 后查询产出批次：`pending_quality/pending`；按批次查询库存流水为空，商品库存余额为空。
- 合格放行后：同一产出只有一条 `produce/inbound` 流水，成品库余额为 `9.500 份`，批次为 `released`，工单为 `completed`。
- 使用原 `Idempotency-Key` 和旧 revision 重复提交放行，仍返回同一检查、流水和余额，没有重复入账。
- 另一批次判定不合格后：产出/库存批次为 `rejected`，工单为 `exception`，按批次查询库存流水为空。
- 数据库触发器禁止删除实际产出、修改产出事实或从终态再次转换；质量检查与库存流水均禁止修改删除。

## 浏览器验证

使用真实 development API 完成桌面 1440×1000 和移动 390×844 验收：

1. 从生产工单开始任务，状态更新为生产中。
2. 打开“报产”，填写真实批次号和有效期，提交后状态变为等待质检。
3. 未填写质量标准时点击放行，页面以 `role=alert` 阻断并说明必填项。
4. 填写标准版本、抽样数量、实测温度并完成外观、封口、标签检查，合格放行成功。
5. 工单列表显示已完成；产出详情显示标准、抽样、温度、成品库位和入库数量。
6. “查看成品库存”进入库存台账，可见同批次 `10.000 份`可用库存和唯一生产入库流水。
7. 移动端为单列卡片/对话框，核心按钮触控高度合格，无横向溢出。
8. 浏览器控制台 0 error、0 warning；Playwright 会话已关闭。

截图保存在本地忽略目录 `output/playwright/work-order-output/.playwright-cli/`，不进入 Git 历史。

## 恢复说明

- 事务失败可保留原幂等键重试，不会留下部分质量、库存或状态事实。
- 代码回滚不删除已经生成的产出、检查或库存流水；数据库结构只能通过后续 migration 恢复。
- production 继续只读；在可信身份和职责权限落地前，所有相关写命令由服务端失败关闭。
