# Nora Roadmap

Roadmap 按领域依赖排序，不承诺未经验证的日期。每阶段必须用真实纵向流程验收，不能只完成页面。

## 阶段 0：边界和治理（当前）

- 收敛产品边界、术语、状态机与 Build / Buy / Integrate。
- 建立 Agent 规则、ADR、任务/PR 模板、CI 和文档链接门禁。
- 修复 BOM 时间语义、审批快照、幂等和 Demo/生产边界。
- 人工完成分支保护与许可证决策；规划大文件迁移，不重写历史。

退出条件：项目事实可从仓库恢复；P0 检查稳定运行；Mock 不会进入生产写路径。

## 阶段 1：食品制造领域底座

- 所有阶段继续遵守净配菜边界；最终加热制作、热菜工位和相关设备控制不进入 Roadmap。
- 组织、工厂、用户和权限上下文。
- Uom/UomCategory/UomConversion 及采购、库存、配方、销售、包装单位。
- 配方版本、工艺步骤、阶段投料、半成品、副产物、替代料、出成率和损耗。
- 已完成首个纵向切片：版本内有序工序、工序级阶段投料、审批快照 v2 和融合工作台；并行/返工、副产物与替代料仍未实现。
- 已完成库存底座、工单领退料，以及工单报产 → 质量判定 → 合格成品入库/不合格异常的首个纵向闭环；采购收货、实际投料、调拨、损耗/报废和完整质检追溯仍未实现。

退出条件：真实业务样例可表达；核心不变量有数据库约束、Policy 和集成测试。

## 阶段 2：订单到生产批次

```text
订单 → 审核 → 配方快照 → 生产需求 → 合批/拆批
    → 物料需求 → 库存检查 → 释放工单
```

- 截单与订单变更影响分析。
- 生产需求合并/拆分、批次编号和来源分摊。
- 不可变批次/工单快照、服务端状态机和幂等释放。
- 已完成第二个纵向切片：兼容需求合批/拆批、草稿批次、批次确认、基于冻结 v2 配方的一批次一工单释放、Decimal 数量、幂等、revision 和审计；多工单拆分、产能排程和 MES 执行仍未实现。

退出条件：至少一个真实订单集合可生成可解释、可重复验证的生产批次和工单。

## 阶段 3：MES、库存和追溯

```text
扫码领料 → 实际投料 → 质量检查 → 实际产出
→ 损耗/退料/返工 → 成品批次 → 库存流水 → 双向追溯
```

- 现场身份、设备、离线同步队列与幂等键。
- 已完成首个现场执行控制切片：工单开始、暂停、继续、异常与恢复的服务端命令、工位/设备上下文和审计；production 可信身份和实际工序投料仍未完成。
- 已完成首个工单物料切片：只按冻结 v2 配方计算叶子需求，基于已放行批次余额完成幂等领料/退料，并记录工单、批次、库位、工位和设备因果链；实际投料和离线扫码仍未完成。
- 已完成首个单批次产出质量切片：报产形成待检批次，质量放行原子追加成品入库并完成工单，不合格进入异常且不入库；部分报产、返工/报废和让步接收仍未完成。
- FEFO、质检状态、HACCP 控制点和不合格处置。
- 正向去向查询、反向原料召回和证据导出。

退出条件：断网、重复提交、部分失败和召回演练均有验证证据。当前已有状态、领退料和单批次产出/质检/入库链，仍缺实际投料、损耗/报废、采购来源、离线队列和完整双向追溯。

## 阶段 4：采购、配送和成本

- 采购建议、到货称重、来料质检和供应商批次。
- 配送波次、装车、签收、退货和差异。
- 标准/实际成本、损耗分摊与外部财务凭证接口。

## 阶段 5：产品化

- 多租户与数据隔离、实施配置、导入导出、API/Webhook。
- 备份恢复、监控告警、审计导出、升级和迁移工具。
- Operations 与 Showroom 构建边界拆分。

## 阶段 6：优化和 AI

顺序固定：

```text
确定性规则 → 数据积累 → 优化求解 → AI 辅助解释
→ 人工确认 → 少量低风险自动化
```

优先评估 OR-Tools/Timefold，不自研通用求解器。没有完整约束、可行性校验和人工回退时，不宣传“AI 自动规划”。
