# Feature Slice Rules

继承 [src/AGENTS.md](../AGENTS.md)。每个功能切片应包含页面、领域展示类型、API 状态和测试，而不是仅新增一个孤立页面。

- 复用现有 AppShell、页面标题、卡片、表格、状态标签、空态和对话框。
- Operations 功能必须说明数据来源和写入路径；Showroom/Demo 功能必须显示演示身份。
- 新路由应有明确入口、成功出口和返回路径。重复提交不得产生重复对象。
- 业务状态来自服务端；组件局部状态只用于表单、选择、展开和显式 Demo。
