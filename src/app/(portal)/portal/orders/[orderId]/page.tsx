import { PortalOrderDetail } from "@/features/portal/portal-pages";export default async function Page({params}:{params:Promise<{orderId:string}>}){const {orderId}=await params;return <PortalOrderDetail id={orderId}/>}
